@* Shared fallback foot assets. Theme can override by adding its own Partials/FootAssets.cshtml *@
@* Include shared base scripts (if any). This partial stays minimal by default. *@
@await Html.PartialAsync("BaseFootAssets")
