// Shared base script (example). Keep minimal and stable.
// This demonstrates that Shared BaseFootAssets wiring works.
(function(){
  try {
    console.debug('[Articulate][Shared] base.js loaded');
  } catch (_) {}
})();
