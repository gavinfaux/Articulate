@model PagerModel

@if (Model is null)
{
    return;
}
<nav class="pager container" aria-label="Pagination">
    <ul>
        <li>
            @if (Model.HasPrevious && !string.IsNullOrEmpty(Model.PreviousUrl))
            {
                <a rel="prev" href="@Model.PreviousUrl" role="button" class="secondary">Newer posts</a>
            }
        </li>
        <li>
            @if (Model.HasNext && !string.IsNullOrEmpty(Model.NextUrl))
            {
                <a rel="next" href="@Model.NextUrl" role="button" class="secondary">Older posts</a>
            }
        </li>
    </ul>
</nav>
