## Archived PRDs

The product requirement drafts that previously lived under `docs/pr/feat` have been retired.

- To reference them, use Git history (for example `git show HEAD^:docs/pr/feat/MarkdownEditor.md`).
- Snapshot tags and release branches keep the original context for long-term reference.

Feel free to reinstate specific PRDs under this folder if a proposal becomes active again.
